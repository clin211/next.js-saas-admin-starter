# =============================================================================
# Docker 构建忽略表（作用于构建上下文 = 仓库根）
# 这些文件不会被 COPY 进镜像，减小上下文体积、避免覆盖容器内产物、杜绝密钥泄漏。
# =============================================================================

# —— 依赖与构建产物（容器内重新生成）——
node_modules
.next
out
coverage
*.tsbuildinfo

# —— 版本控制与编辑器 ——
.git
.gitignore
.gitattributes
.vscode
.idea
.DS_Store

# —— 本地环境与密钥（绝不进镜像）——
.env
.env.*
*.pem
*.key

# —— 日志 ——
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# —— 文档与脚手架元数据（运行期不需要）——
docs
README.md
AGENTS.md
CLAUDE.md

# —— AI 工具配置（与运行无关）——
.claude
.agents
skills-lock.json
